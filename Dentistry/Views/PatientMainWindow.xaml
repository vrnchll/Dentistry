<Window x:Class="Dentistry.PatientMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dentistry" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid>
        <StackPanel Margin="0,0,640,0" Orientation="Vertical">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0.16"/>
                    <GradientStop Color="#FF607D8B" Offset="0.961"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <Button Height="42" Command="{Binding AddNewCompoun}">
                <Grid>
                    <materialDesign:PackIcon Kind="Edit" RenderTransformOrigin="-1,0.812" ></materialDesign:PackIcon>
                    <TextBlock Text="Заказать талон" Margin="21,0,0,0"/>
                </Grid>
            </Button>
            <Button Height="42" Command="{Binding ShowDoctors}">
                <Grid>
                    <materialDesign:PackIcon Kind="Doctor" RenderTransformOrigin="-1,0.812" ></materialDesign:PackIcon>
                    <TextBlock Text="Врачи" Margin="21,0,0,0"/>
                </Grid>
            </Button>
            <Button Height="42" Command="{Binding ShowServices}">
                <Grid>
                    <materialDesign:PackIcon Kind="ServiceToolbox" RenderTransformOrigin="-1,0.812" ></materialDesign:PackIcon>
                    <TextBlock Text="Услуги" Margin="21,0,0,0"/>
                </Grid>
            </Button>
            <Button Height="42" Command="{Binding ShowProfile}">
                <Grid>
                    <materialDesign:PackIcon Kind="Account" RenderTransformOrigin="-1,0.812" ></materialDesign:PackIcon>
                    <TextBlock Text="Профиль" Margin="21,0,0,0"/>
                </Grid>
            </Button>
        </StackPanel>
        <Frame Margin="152,0,0,0" 
               Content="{Binding CurrentPage}"
               />
    </Grid>
</Window>
